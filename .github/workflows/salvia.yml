name: Salvia SPA

on:
  push:
    branches: [main]
    paths:
      - "salvia/**"
      - "internal/salvia/**"
      - "internal/http/**"
      - "internal/app/**"
      - "Dockerfile"
      - "docs/salvia/**"
      - ".github/workflows/salvia.yml"
  pull_request:
    paths:
      - "salvia/**"
      - "internal/salvia/**"
      - "internal/http/**"
      - "internal/app/**"
      - "Dockerfile"
      - "docs/salvia/**"
      - ".github/workflows/salvia.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: salvia
    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v6.0.9
        with:
          package_json_file: salvia/package.json

      - uses: actions/setup-node@v6.4.0
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: salvia/pnpm-lock.yaml

      - run: pnpm install --frozen-lockfile
      - run: pnpm lint
      - run: pnpm test
      - run: pnpm build
      - name: Verify the embedded build has no CSS assets
        run: |
          test -f ../internal/salvia/dist/index.html
          if find ../internal/salvia/dist -type f -name '*.css' -print -quit | grep -q .; then
            echo 'Salvia emitted a CSS file' >&2
            exit 1
          fi
